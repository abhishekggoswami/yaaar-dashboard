# Pooled Neon PostgreSQL URL used by the server-side Prisma client. Do not commit .env files.
DATABASE_URL=

# Direct/unpooled Neon PostgreSQL URL used only by Prisma CLI migrations.
DIRECT_DATABASE_URL=

# Public application URL used by future server-side integrations.
NEXT_PUBLIC_APP_URL="http://localhost:3000"
